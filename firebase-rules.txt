{
  "rules": {
    "rooms": {
      "$room": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['clipboard']) || newData.hasChildren(['history'])",
        "clipboard": {
          ".validate": "newData.hasChildren(['text', 'ts', 'encrypted']) && newData.child('ts').isNumber() && newData.child('encrypted').isBoolean()"
        },
        "history": {
          "$item": {
            ".validate": "newData.hasChildren(['text', 'ts', 'encrypted']) && newData.child('ts').isNumber() && newData.child('encrypted').isBoolean()"
          }
        }
      }
    }
  }
}